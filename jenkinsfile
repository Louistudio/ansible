pipeline{
    agent: ansible
    stages{
        stag(git checkout){
            step{
                git branch: 'main'
                    url: 'git@github.com:Louistudio/ansible.git'
            }
        }
    }
}